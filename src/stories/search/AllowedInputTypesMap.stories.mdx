import { Meta, ArgsTable } from '@storybook/addon-docs';
import { ConditionalRowStyleInterface } from '../type-components';

<Meta title="Search UI/Allowed Input Types Map" />

# Allowed Input Types Map

## Example

This is an example value for the `SearchUI` prop `searchBarAllowedInputTypesMap`:

```js
{
  formula: {
    field: 'formula'
  },
  elements: {
    field: 'elements'
  },
  mpid: {
    field: 'material_ids'
  }
}
```
